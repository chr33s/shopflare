diff --git a/node_modules/@shopify/app-bridge-types/dist/shopify.ts b/node_modules/@shopify/app-bridge-types/dist/shopify.ts
index 007d159..3652a17 100644
--- a/node_modules/@shopify/app-bridge-types/dist/shopify.ts
+++ b/node_modules/@shopify/app-bridge-types/dist/shopify.ts
@@ -238,17 +238,18 @@ interface CollectionRule {
     relation: string;
 }
 
-enum CollectionSortOrder {
-    Manual = "MANUAL",
-    BestSelling = "BEST_SELLING",
-    AlphaAsc = "ALPHA_ASC",
-    AlphaDesc = "ALPHA_DESC",
-    PriceDesc = "PRICE_DESC",
-    PriceAsc = "PRICE_ASC",
-    CreatedDesc = "CREATED_DESC",
-    Created = "CREATED",
-    MostRelevant = "MOST_RELEVANT"
-}
+const CollectionSortOrder = {
+    Manual: "MANUAL",
+    BestSelling: "BEST_SELLING",
+    AlphaAsc: "ALPHA_ASC",
+    AlphaDesc: "ALPHA_DESC",
+    PriceDesc: "PRICE_DESC",
+    PriceAsc: "PRICE_ASC",
+    CreatedDesc: "CREATED_DESC",
+    Created: "CREATED",
+    MostRelevant: "MOST_RELEVANT"
+} as const;
+type CollectionSortOrder = (typeof CollectionSortOrder)[keyof typeof CollectionSortOrder];
 
 interface Customer {
     /**
@@ -414,11 +415,12 @@ interface Filters {
 
 type Finish = (data?: any) => void;
 
-enum FulfillmentServiceType {
-    GiftCard = "GIFT_CARD",
-    Manual = "MANUAL",
-    ThirdParty = "THIRD_PARTY"
-}
+const FulfillmentServiceType = {
+    GiftCard: "GIFT_CARD",
+    Manual: "MANUAL",
+    ThirdParty: "THIRD_PARTY"
+} as const;
+type FulfillmentServiceType = (typeof FulfillmentServiceType)[keyof typeof FulfillmentServiceType];
 
 interface Header {
     /**
@@ -816,11 +818,12 @@ export interface Product extends Resource {
     status: ProductStatus;
 }
 
-enum ProductStatus {
-    Active = "ACTIVE",
-    Archived = "ARCHIVED",
-    Draft = "DRAFT"
-}
+const ProductStatus = {
+    Active: "ACTIVE",
+    Archived: "ARCHIVED",
+    Draft: "DRAFT"
+} as const;
+type ProductStatus = (typeof ProductStatus)[keyof typeof ProductStatus];
 
 export interface ProductVariant extends Resource {
     availableForSale: boolean;
@@ -857,16 +860,18 @@ export interface ProductVariant extends Resource {
     updatedAt: string;
 }
 
-enum ProductVariantInventoryManagement {
-    Shopify = "SHOPIFY",
-    NotManaged = "NOT_MANAGED",
-    FulfillmentService = "FULFILLMENT_SERVICE"
-}
+const ProductVariantInventoryManagement = {
+    Shopify: "SHOPIFY",
+    NotManaged: "NOT_MANAGED",
+    FulfillmentService: "FULFILLMENT_SERVICE"
+} as const;
+type ProductVariantInventoryManagement = (typeof ProductVariantInventoryManagement)[keyof typeof ProductVariantInventoryManagement];
 
-enum ProductVariantInventoryPolicy {
-    Deny = "DENY",
-    Continue = "CONTINUE"
-}
+const ProductVariantInventoryPolicy = {
+    Deny: "DENY",
+    Continue: "CONTINUE"
+} as const;
+type ProductVariantInventoryPolicy = (typeof ProductVariantInventoryPolicy)[keyof typeof ProductVariantInventoryPolicy];
 
 type Progress = 'incomplete' | 'partiallyComplete' | 'complete';
 
@@ -1366,12 +1371,13 @@ interface WebVitalsMetric {
     value: number;
 }
 
-enum WeightUnit {
-    Kilograms = "KILOGRAMS",
-    Grams = "GRAMS",
-    Pounds = "POUNDS",
-    Ounces = "OUNCES"
-}
+const WeightUnit = {
+    Kilograms: "KILOGRAMS",
+    Grams: "GRAMS",
+    Pounds: "POUNDS",
+    Ounces: "OUNCES"
+} as const;
+type WeightUnit = (typeof WeightUnit)[keyof typeof WeightUnit];
 
 type WithSelection<T> = T & {
     /**
